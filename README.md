# Shuchen Sun Academic Profile

Static academic personal homepage for Shuchen Sun.

## Local Preview

Open `index.html` directly in a browser, or run:

```bash
python3 -m http.server 4173
```

Then visit `http://localhost:4173/`.

## GitHub Pages

Deploy from the repository `main` branch, root directory.
